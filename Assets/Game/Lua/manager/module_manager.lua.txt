---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Jonny.
--- DateTime: 2021/1/17 11:49 下午
---

local ModuleManager = typesys.def.ModuleManager {
    opened_ui = typesys.array
}

function ModuleManager:__ctor()
    self.opened_ui = typesys.new(typesys.array, typesys.Panel)
end

function ModuleManager:open_ui(panel)
    --self.opened_ui:pushFront(panel)
end

function ModuleManager:close_ui(panel)
    --local size = self.opened_ui:size()
    --for index = size, 1, -1 do
    --    local p = self.opened_ui:get(index)
    --    if p == panel then
    --        self.opened_ui:set(index, nil)
    --        return
    --    end
    --end
end