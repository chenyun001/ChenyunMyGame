---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Jonny.
--- DateTime: 2021/1/18 16:03
---

local new = typesys.new
require "module/base/aa_cache"

---@class Panel:UIEventBase
local Panel = typesys.def.Panel {
    __super = typesys.UIEventBase,

    ---@type AACache
    _aa_cache = typesys.AACache,

    gameObject = typesys.__unmanaged,
    transform = typesys.__unmanaged,
    this = typesys.__unmanaged,

    param = typesys.__unmanaged,
    start_complete = false
}

function Panel:__ctor(...)
    self.__owner = true
    self:_init_cache()
end

function Panel:awake()
    
end

function Panel:start()

end
function Panel:resume()

end
function Panel:update(delta_time)

end

function Panel:add_aa_handle(aa_handle)
    if self._aa_cache == nil then
        self._aa_cache = new(typesys.AACache)
    end
    self._aa_cache:add_aa_handle(aa_handle)
end

function Panel:_release_aa_handle()
    if self._aa_cache ~= nil then
        self._aa_cache:release_aa_handle()
    end
end

function Panel:destroy()
    self:remove_listeners()
    self:_release_aa_handle()
    self.__owner = false
end

function Panel:release()
    self:remove_listeners()
    self:_release_aa_handle()
    self.__owner = false
end

return Panel