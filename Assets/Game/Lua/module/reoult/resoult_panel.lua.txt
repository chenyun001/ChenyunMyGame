---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by chenyun.
--- DateTime: 2022/10/27 
---
local new = typesys.new
local delete = typesys.delete
local GameObject = CS.UnityEngine.GameObject
local game_events = require("module/game_events")
local role_model = lua_framework.proxy.player_info_proxy
local meta = typesys.def.ResoultPanel {
    __super = typesys.Panel,
    Btn_PlayGame = typesys.__unmanaged,
    Text_value = typesys.__unmanaged,
    role = typesys.__unmanaged,
    GameBattle = typesys.__unmanaged,
}

function meta:awake()
    self:add_listeners()
    -- self.GameBattle =  GameObject.Find('GameBattle'):GetComponent(typeof(CS.GameBattle))
    --self.role =  GameObject.Find('role'):GetComponent(typeof(CS.Role))
end

function meta:start()
    -- self.Text_rank.text = role_model.rankIndex
    -- self.Text_value.text = role_model.resoultCount
end

function meta:add_listeners()
    self.Btn_PlayGame.onClick:AddListener(function()
        self:enter_game()
    end)
end

function meta:enter_game()
    -- self.GameBattle.resoultCount = 0
    -- self.GameBattle:GameResSet()
    -- self.role:GameResSet()
    GameUIManager:OpenUI("MainPanel.prefab")
    GameUIManager:CloseUI("ResoultPanel.prefab")
end

function meta:destroy() 
    self:release()
end

return meta